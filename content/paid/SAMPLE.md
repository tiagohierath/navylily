# Sample paid lesson

exemplo placeholder

This is a placeholder. Real paid lessons are **not** part of this open-source
repository — they are kept private and provided at deploy time (see the README).

The build pipeline turns `content/paid/*.md` into `protected/*.html`, which the
server serves only to logged-in, active members via `/protected/`.
